<% const params = {
  layer: '',
  title: '',
  dirname: false,
  un_id: 'index',
  un_canonical: ''
}; %>

<%- include('/_head', { layer: params.layer, title: params.title, dirname: params.dirname, un_id: params.un_id,un_canonical: params.un_canonical }); %>

<%- include('/_foot', { layer: params.layer, title: params.title, dirname: params.dirname, un_id: params.un_id,un_canonical: params.un_canonical }); %>
